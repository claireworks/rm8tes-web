.DS_Store
Thumbs.db
*.log

# This repository is public, so committing a file publishes it on github.com
# whatever Pages does with it -- `_config.yml` keeps working documents off
# rm8tes.com, but only .gitignore keeps them off the internet. Notes describing
# weaknesses that are not all closed yet, and unreviewed legal drafts, stay out.
SECURITY_AUDIT.md
TERMS_HOUSEHOLD_DRAFT.md
